<Window x:Class="TouchGanttChart.Views.ProjectTemplateSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TouchGanttChart.Views"
        mc:Ignorable="d"
        Title="Create Project from Template" 
        Height="700" 
        Width="1000"
        MinHeight="600"
        MinWidth="800"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        IsManipulationEnabled="True"
        Background="#f8f9fa">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        
        <!-- Touch-optimized button style -->
        <Style x:Key="TouchButtonStyle" TargetType="Button">
            <Setter Property="MinHeight" Value="44"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#007bff"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0056b3"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#6c757d"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Template card style -->
        <Style x:Key="TemplateCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#dee2e6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="MinHeight" Value="120"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#007bff"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Selected template card style -->
        <Style x:Key="SelectedTemplateCardStyle" TargetType="Border" BasedOn="{StaticResource TemplateCardStyle}">
            <Setter Property="BorderBrush" Value="#007bff"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="Background" Value="#e3f2fd"/>
        </Style>
        
        <!-- Input field style -->
        <Style x:Key="TouchInputStyle" TargetType="TextBox">
            <Setter Property="MinHeight" Value="44"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#ced4da"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        
        <!-- ComboBox style -->
        <Style x:Key="TouchComboBoxStyle" TargetType="ComboBox">
            <Setter Property="MinHeight" Value="44"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#ced4da"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Create New Project" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       Foreground="#212529"
                       Margin="0,0,0,8"/>
            <TextBlock Text="Choose a template to get started quickly with predefined tasks and structure"
                       FontSize="16" 
                       Foreground="#6c757d"/>
        </StackPanel>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Search and Filter Bar -->
            <Border Grid.Row="0" 
                    Background="White"
                    BorderBrush="#dee2e6"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="16"
                    Margin="0,0,0,16">
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Search -->
                    <TextBox Grid.Column="0"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource TouchInputStyle}"
                             Margin="0,0,12,0">
                        <TextBox.Resources>
                            <Style TargetType="TextBox" BasedOn="{StaticResource TouchInputStyle}">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="🔍 Search templates..." 
                                                                   Foreground="#adb5bd" 
                                                                   FontSize="14"
                                                                   Margin="12,0,0,0"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>
                    
                    <!-- Category Filter -->
                    <TextBlock Grid.Column="1" 
                               Text="Category:" 
                               VerticalAlignment="Center"
                               FontWeight="SemiBold"
                               Margin="0,0,8,0"/>
                    
                    <ComboBox Grid.Column="2"
                              ItemsSource="{Binding Categories}"
                              SelectedItem="{Binding SelectedCategory}"
                              Style="{StaticResource TouchComboBoxStyle}"
                              Margin="0,0,12,0"/>
                    
                    <!-- Popular Filter -->
                    <CheckBox Grid.Column="3"
                              IsChecked="{Binding ShowPopularOnly}"
                              Content="Popular Only"
                              VerticalAlignment="Center"
                              FontSize="14"
                              Margin="0,0,12,0"/>
                    
                    <!-- Clear Filters -->
                    <Button Grid.Column="4"
                            Content="Clear"
                            Command="{Binding ClearFiltersCommand}"
                            Style="{StaticResource TouchButtonStyle}"
                            Background="#6c757d"
                            MinWidth="80"/>
                </Grid>
            </Border>
            
            <!-- Templates List -->
            <Border Grid.Row="1" 
                    Background="White"
                    BorderBrush="#dee2e6"
                    BorderThickness="1"
                    CornerRadius="8">
                
                <!-- Templates ScrollViewer -->
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              IsManipulationEnabled="True"
                              Padding="8">
                    
                    <Grid>
                        <!-- No Templates Message -->
                        <StackPanel VerticalAlignment="Center" 
                                    HorizontalAlignment="Center"
                                    Visibility="{Binding HasNoTemplates, Converter={StaticResource BoolToVisConverter}}"
                                    Margin="40">
                            
                            <TextBlock Text="📋" 
                                       FontSize="48" 
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,16"/>
                            
                            <TextBlock Text="No templates found" 
                                       FontSize="18" 
                                       FontWeight="SemiBold"
                                       Foreground="#6c757d"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,8"/>
                            
                            <TextBlock Text="Try adjusting your search criteria or create a new project from scratch" 
                                       FontSize="14"
                                       Foreground="#adb5bd"
                                       HorizontalAlignment="Center"
                                       TextAlignment="Center"/>
                        </StackPanel>
                        
                        <!-- Templates Grid -->
                        <ItemsControl ItemsSource="{Binding FilteredTemplates}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Name="TemplateCard" 
                                        Style="{StaticResource TemplateCardStyle}"
                                        MouseLeftButtonDown="OnTemplateCardClick"
                                        Tag="{Binding}">
                                    
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        
                                        <!-- Template Header -->
                                        <StackPanel Grid.Row="0" 
                                                    Orientation="Horizontal"
                                                    Margin="0,0,0,8">
                                            <TextBlock Text="{Binding Icon}" 
                                                       FontSize="24" 
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,12,0"/>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}"
                                                           FontSize="16"
                                                           FontWeight="Bold"
                                                           Foreground="#212529"/>
                                                <TextBlock Text="{Binding Category}"
                                                           FontSize="12"
                                                           Foreground="#6c757d"/>
                                            </StackPanel>
                                        </StackPanel>
                                        
                                        <!-- Template Description -->
                                        <TextBlock Grid.Row="1"
                                                   Text="{Binding Description}"
                                                   FontSize="13"
                                                   Foreground="#495057"
                                                   TextWrapping="Wrap"
                                                   MaxHeight="60"
                                                   Margin="0,0,0,12"/>
                                        
                                        <!-- Template Stats -->
                                        <StackPanel Grid.Row="3" 
                                                    Orientation="Horizontal"
                                                    HorizontalAlignment="Left">
                                            <StackPanel Orientation="Horizontal" 
                                                        Margin="0,0,16,0">
                                                <TextBlock Text="📅" FontSize="12" Margin="0,0,4,0"/>
                                                <TextBlock Text="{Binding DurationDisplay}" 
                                                           FontSize="12" 
                                                           Foreground="#6c757d"/>
                                            </StackPanel>
                                            
                                            <StackPanel Orientation="Horizontal" 
                                                        Margin="0,0,16,0">
                                                <TextBlock Text="📋" FontSize="12" Margin="0,0,4,0"/>
                                                <TextBlock Text="{Binding TaskCount}" 
                                                           FontSize="12" 
                                                           Foreground="#6c757d"/>
                                                <TextBlock Text=" tasks" 
                                                           FontSize="12" 
                                                           Foreground="#6c757d"/>
                                            </StackPanel>
                                            
                                            <StackPanel Orientation="Horizontal"
                                                        Visibility="{Binding BudgetDisplay, Converter={StaticResource BoolToVisConverter}}">
                                                <TextBlock Text="💰" FontSize="12" Margin="0,0,4,0"/>
                                                <TextBlock Text="{Binding BudgetDisplay}" 
                                                           FontSize="12" 
                                                           Foreground="#6c757d"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                
                                <DataTemplate.Triggers>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=local:ProjectTemplateSelectionDialog}, Path=DataContext.SelectedTemplate}" Value="{Binding}">
                                        <Setter TargetName="TemplateCard" Property="Style" Value="{StaticResource SelectedTemplateCardStyle}"/>
                                    </DataTrigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    </Grid>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Bottom Panel - Project Details and Actions -->
        <Border Grid.Row="2" 
                Background="White"
                BorderBrush="#dee2e6"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20"
                Margin="0,20,0,0">
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Project Details Form -->
                <StackPanel Grid.Column="0" 
                            Margin="0,0,20,0"
                            IsEnabled="{Binding HasSelectedTemplate}">
                    
                    <TextBlock Text="Project Details" 
                               FontSize="18" 
                               FontWeight="Bold"
                               Foreground="#212529"
                               Margin="0,0,0,16"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Project Name -->
                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Project Name *" 
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,4"/>
                        <TextBox Grid.Row="1" Grid.Column="0"
                                 Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource TouchInputStyle}"
                                 Margin="0,0,0,12"/>
                        
                        <!-- Project Manager -->
                        <TextBlock Grid.Row="0" Grid.Column="2"
                                   Text="Project Manager *" 
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,4"/>
                        <TextBox Grid.Row="1" Grid.Column="2"
                                 Text="{Binding ProjectManager, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource TouchInputStyle}"
                                 Margin="0,0,0,12"/>
                        
                        <!-- Start Date -->
                        <TextBlock Grid.Row="2" Grid.Column="0"
                                   Text="Start Date" 
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,4"/>
                        <DatePicker Grid.Row="3" Grid.Column="0"
                                    SelectedDate="{Binding StartDate}"
                                    MinHeight="44"
                                    FontSize="14"
                                    Margin="0,0,0,12"/>
                        
                        <!-- Estimated End Date -->
                        <TextBlock Grid.Row="2" Grid.Column="2"
                                   Text="Estimated End Date" 
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,4"/>
                        <TextBox Grid.Row="3" Grid.Column="2"
                                 Text="{Binding EstimatedEndDate, StringFormat='yyyy-MM-dd'}"
                                 Style="{StaticResource TouchInputStyle}"
                                 IsReadOnly="True"
                                 Background="#f8f9fa"
                                 Margin="0,0,0,12"/>
                    </Grid>
                    
                    <!-- Selected Template Info -->
                    <TextBlock Text="{Binding SelectedTemplateInfo}"
                               FontSize="14"
                               Foreground="#6c757d"
                               Visibility="{Binding HasSelectedTemplate, Converter={StaticResource BoolToVisConverter}}"/>
                </StackPanel>
                
                <!-- Action Buttons -->
                <StackPanel Grid.Column="1" 
                            Orientation="Vertical">
                    
                    <Button Content="Create Project" 
                            Command="{Binding CreateProjectCommand}"
                            Style="{StaticResource TouchButtonStyle}"
                            Background="#28a745"
                            MinWidth="160"
                            Margin="0,0,0,8"/>
                    
                    <Button Content="Cancel" 
                            Command="{Binding CancelCommand}"
                            Style="{StaticResource TouchButtonStyle}"
                            Background="#6c757d"
                            MinWidth="160"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3"
                Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}">
            <StackPanel VerticalAlignment="Center" 
                        HorizontalAlignment="Center">
                <ProgressBar Width="200" 
                             Height="8"
                             IsIndeterminate="True"
                             Margin="0,0,0,16"/>
                <TextBlock Text="{Binding StatusMessage}" 
                           FontSize="16"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>